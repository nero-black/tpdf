<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tpdf.shoot.service.member">

	<!-- parameterType & resultType -->
	
	<select id="login_check" resultType="MemberVo">
		select * from member_tbl member_tbl where member_id=#{member_id} and member_pw=#{member_pw}
	</select>
	
	<insert id="join_process" parameterType="MemberVo">
		insert into member_tbl
		(member_id, member_pw, member_name, member_gender, member_birth, member_addr, member_email, member_tel) values
		(#{member_id}, #{member_pw}, #{member_name}, #{member_gender}, #{member_birth}, #{member_addr}, #{member_email}, #{member_tel})
	</insert> 

	<select id="id_check" parameterType="MemberVo" resultType="int">
		select exists ( select member_idx from member_tbl where member_id=#{id_check} ) as checker
	</select>

</mapper>	


